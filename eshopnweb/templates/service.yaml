apiVersion: v1
kind: Service
metadata:
  name: {{ include "eshopnweb.fullname" . }}
  labels:
    {{- include "eshopnweb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eshopnweb.selectorLabels" . | nindent 4 }}
