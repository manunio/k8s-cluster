apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eshopnweb.fullname" . }}-test-connection"
  labels:
    {{- include "eshopnweb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eshopnweb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
